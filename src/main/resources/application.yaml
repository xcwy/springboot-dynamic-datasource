server:
  port: 8089

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rai
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
    hibernate:
      ddl-auto: update